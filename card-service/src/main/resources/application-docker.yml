spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/card_db
    username: postgres
    password: postgres
  data:
    redis:
      host: redis
      port: 6379
  cloud:
    aws:
      region:
        static: us-east-1
      credentials:
        access-key: test
        secret-key: test
      sqs:
        endpoint: http://localstack:4566

app:
  clients:
    product-service:
      base-url: http://product-service:8081
